@model IEnumerable<Book>

@if(Model.Count() == 0)
{
    <p>Brak książek w zbiorze</p>
}

@foreach (Book book in Model)
{
    <div class="item">
        @Html.Action("ShowBook", book)
        <br />
    </div>
}